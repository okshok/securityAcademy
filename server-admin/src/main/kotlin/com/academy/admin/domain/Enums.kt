package com.academy.admin.domain

enum class QuestionStatus { DRAFT, OPEN, CLOSED, RESOLVED }
enum class Outcome { O, X, VOID }
enum class CandidateStatus { CANDIDATE, SELECTED, DISCARDED }
enum class QuestionType { INDEX, EARNINGS, MACRO }
